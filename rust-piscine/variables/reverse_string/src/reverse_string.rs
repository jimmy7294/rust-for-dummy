pub fn rev_str(s: &str) -> String {
    // s.chars().rev().collect()
    // let mut res = String::new();

    // or
    s.chars().rev().collect::<String>()
    // or
    // for c in s.chars().rev() {
    //     res.push(c);
    // }
    // res
}
